# Frontend (Vite + React + Tailwind)

Setup and run the frontend locally:

```bash
cd frontend
npm install
npm run dev
```

This project uses Vite, React and Tailwind. Tailwind PostCSS plugin is configured in `postcss.config.cjs`.
